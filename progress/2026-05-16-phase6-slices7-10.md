# Phase 6 — Profile Integration: Slices 7–10 Complete

**Date:** 2026-05-16  
**Status:** Phase 6 complete. 62/63 C++ tests (1 LLVM skip), 46/46 TS tests, 0 type errors.

## Slice 7: Recording UI
- `extension/src/panels/recordProfilePanel.ts` — singleton webview: target binary, args, label, preset (hotspots/memory/branches/frontend), frequency; assembles `perf record` command, spawns via `child_process.exec`, auto-imports on completion
- `perfLens.recordProfile` command registered in extension.ts + package.json
- Linux-only with clear warning in UI; gracefully errors on non-Linux systems

## Slice 8: Profile-derived rules (C++ sidecar)
- `sidecar/src/rules/packs/profile_driven/profile_rules.hpp/.cpp` — 4 rules:
  - `perf-lens.profile.cache-miss-hotspot` — ≥5% cycles + ≥10% L1 misses at same line
  - `perf-lens.profile.branch-mispredict` — ≥10% branch-misses + ≥3% cycles
  - `perf-lens.profile.memory-bandwidth-bound` — ≥5% cycles + ≥15% LLC misses
  - `perf-lens.profile.low-ipc` — ≥10% cycles + no elevated L1 miss (compute-bound heuristic)
- `analyseFile` RPC now accepts optional `profileId`; appends profile-derived findings when provided
- `sidecar/tests/profile_rules_test.cpp` — 8 tests; all pass

## Slice 9: Staleness detection + comparison
- `ProfileManager.checkStaleness(uri)` — hashes current file, compares to stored hash; shows banner with "Re-profile / Show Profile Panel / Dismiss" on mismatch
- `ProfileManager.snapshotSourceHashes(files)` — stores SHA-256 hashes of source files at import time
- Sidecar: `getSourceHashes` + `storeSourceHashes` RPC methods
- `extension/src/panels/profileComparePanel.ts` — two-step webview: profile selector → comparison table with Δ column, regressions highlighted red, improvements green; regression threshold = 2pp
- `perfLens.compareProfiles` command

## Slice 10: LLM synthesis
- `HotnessContext` + `SynthesisContext` types added to `types.ts`
- `buildExplainHotnessRequest(ctx)` — replaces stub; rich prompt with top-5 functions + static findings, produces plain-prose 3-paragraph analysis
- `buildSynthesiseTopFindingsRequest(ctx)` — new; top-3 ranked recommendations as JSON `{rank, function, expectedImpact, action, evidence}`
- `LLMManager.explainHotness()` + `LLMManager.synthesiseTopFindings()` methods
- `perfLens.synthesiseHotness` command — queries top functions, opens ExplanationPanel, streams synthesis

## Commands added this session
- `perfLens.recordProfile`
- `perfLens.compareProfiles`
- `perfLens.synthesiseHotness`

## Phase 6 exit criteria check
- ✅ Record profile via IDE + auto-import
- ✅ Gutter heatmap (slice 4)
- ✅ Suggestions re-sort by hotness (slice 6)
- ✅ Staleness banner when source diverges
- ✅ Comparison mode with regression highlight
- ✅ LLM synthesises top-3 recommendations from profile

**Phase 6 complete. Next: Phase 7 — Static Checks v2, Polish, Release.**
