# Phase 6 — Profile Integration: Slices 1–6 Complete

**Date:** 2026-05-16  
**Status:** 54/55 C++ tests pass (1 LLVM skip), 46/46 TS tests pass, 0 type errors.

## What was built

### Slice 1: ProfileStore (C++ sidecar)
- `sidecar/src/profile/model.hpp` — ProfileMetadata, Sample, Frame, LineHotness, FunctionHotness, ImportedLineRow, ImportedFunctionRow, SourceFileHash
- `sidecar/src/profile/store.hpp/.cpp` — SQLite-backed store: profiles, line_hotness, function_hotness, source_hashes tables; ingestFromNdjson(), insertLineRows(), getLineHotness(), getFileHotness(), getTopFunctions(), getSourceHashes()
- `sidecar/tests/profile_store_test.cpp` — 10 tests

### Slice 2: Rust importers
- `profile-tools/perf-importer/` — drives `perf script`, aggregates self/total counts, emits NDJSON (metadata + line + function rows). Includes unit tests for parser and aggregator.
- `profile-tools/pprof-importer/` — decodes gzipped protobuf pprof files, emits same NDJSON schema. Custom string_table extraction bypasses incorrect proto field type.
- Cargo unavailable in this env — binaries build with `cargo build --release` on a Rust-capable machine.

### Slice 3: RPC methods (sidecar main.cpp)
- Added ProfileStore instance at `$workspace/.perf-lens/profiles.sqlite`
- New methods: `importProfile`, `listProfiles`, `deleteProfile`, `getLineHotness`, `getFileHotness`, `getTopFunctions`
- `importProfile` spawns importer binary (searches PATH + ~/.cargo/bin), reads NDJSON via custom streambuf; falls back to direct NDJSON ingest if binary not found
- Capabilities now includes `"profileIntegration"`
- Version bumped to 0.5.0

### Slice 4: TypeScript types + ProfileManager + GutterHeatmapProvider
- `protocol.ts` additions: ProfileMetadata, LineHotness, FunctionHotness, plus all param/result types
- `extension/src/profile/profileManager.ts` — importProfile(), deleteProfile(), setActiveProfile(), getLineHotness() (cached), getFileHotness(), getTopFunctions(), onProfileChanged event
- `extension/src/profile/hotnessProvider.ts` — 4-bucket gutter decorations (trace/warm/hot/critical), updates on editor change + profile change
- `extension/src/profile/profileManager.test.ts` — 7 tests

### Slice 5: ProfilePanel webview
- `extension/src/panels/profilePanel.ts` — singleton panel, lists profiles, top-20 functions, Import/SetActive/Delete buttons, refreshes on profile change

### Slice 6: Suggestion prioritisation
- `FindingsDiagnosticProvider.setProfileManager()` — when a profile is active, hot findings have severity upgraded/downgraded and a `[X.X% cycles]` suffix; diagnostics sorted by hotness descending
- `extension.ts` — ProfileManager + GutterHeatmapProvider instantiated after sidecar ready; `perfLens.showProfilePanel` and `perfLens.importProfile` commands registered
- `package.json` v0.5.0, 2 new commands

## Mock fixes
- `EventEmitter` mock now properly wires listeners and calls them on `fire()`
- Added: `window.showOpenDialog`, `window.withProgress`, `window.createTextEditorDecorationType`, `ProgressLocation`, `OverviewRulerLane`

## Pending (slices 7–10)
- Recording UI (`perfLens.recordProfile` command)
- Profile-derived rules in sidecar
- Staleness detection + banner
- Comparison mode (ProfileComparePanel)
- LLM synthesis (`explain_hotness` prompt)
